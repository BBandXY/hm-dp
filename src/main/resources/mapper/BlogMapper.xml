<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hmdp.mapper.BlogMapper">

    <!-- 所有字段均由服务端生成且长度受控；IGNORE 仅用于把批次主键冲突转成返回值 0。 -->
    <insert id="insertLikeSyncBatch">
        INSERT IGNORE INTO tb_blog_like_sync_batch (batch_id, item_count)
        VALUES (#{batchId}, #{itemCount})
    </insert>

    <update id="batchIncrementLiked">
        UPDATE tb_blog
        SET liked = GREATEST(
            CAST(COALESCE(liked, 0) AS SIGNED) +
            CASE id
                <foreach collection="items" item="item">
                    WHEN #{item.blogId} THEN #{item.delta}
                </foreach>
                ELSE 0
            END,
            0
        )
        WHERE id IN
        <foreach collection="items" item="item" open="(" separator="," close=")">
            #{item.blogId}
        </foreach>
    </update>

    <update id="batchReconcileLiked">
        UPDATE tb_blog
        SET liked = CASE id
            <foreach collection="items" item="item">
                WHEN #{item.blogId} THEN #{item.liked}
            </foreach>
            ELSE liked
        END
        WHERE
        <foreach collection="items" item="item" separator=" OR ">
            (id = #{item.blogId} AND COALESCE(CAST(liked AS SIGNED), -1) &lt;&gt; #{item.liked})
        </foreach>
    </update>

    <delete id="deleteLikeSyncBatchesBefore">
        DELETE FROM tb_blog_like_sync_batch
        WHERE create_time &lt; #{cutoff}
        <if test="activeBatchId != null and activeBatchId != ''">
            AND batch_id &lt;&gt; #{activeBatchId}
        </if>
    </delete>
</mapper>
